apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: devopstask-pv-claim
  namespace: devopstask
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
